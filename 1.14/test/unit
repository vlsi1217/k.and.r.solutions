#!/usr/bin/env roundup

source helpers

describe "character histogram"

it_should_chart_two_os_in_foo() {
  echo foo | 1.14 | grep "o: ██"
}

it_should_chart_one_f_in_foo() {
  echo foo | 1.14 | grep "f: █"
}

